- hosts: localhost
  tasks: 
  - name: Example playbook
    ansible.builtin.debug:
      msg: "I'm calling this awesome playbook from a collection {{ name }}"
  - name: include more tasks
    ansible.builtin.include_tasks: tasks.yml
